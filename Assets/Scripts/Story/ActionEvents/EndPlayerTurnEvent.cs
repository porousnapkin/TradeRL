using UnityEngine;

#warning "no such thing as turns on world map. replace?"
public class EndPlayerTurnEvent : StoryActionEvent {

	//TODO: Not turns on the map. this needs a new identity.
	public void Activate() {
		//controller.EndTurn();
	}		
}